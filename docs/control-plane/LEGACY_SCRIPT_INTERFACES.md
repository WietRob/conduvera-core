# Legacy Script Interfaces

Dokumentation der 9 CuraOps_VRP-Shell-Skripte. Automatisch extrahiert, nur lesen -- nichts aendern.

> Stand: 2026-05-05

| Script | Exit 0 | Exit != 0 | Args | Env | Output | Sources | Beschreibung |
|--------|--------|-----------|------|-----|--------|---------|--------------|
| `scripts/agent/agent-status.sh` | Status-Token (ACK/BLOCKER/READY_FOR_REVIEW) erfolgreich an GitHub-Issue gepostet; Stream-State-JSON aktualisiert | `1` = falsche Args/ungueltiger Token/Stream nicht gefunden; `10` = unvollstaendige Session-Binding; `12` = Session-Binding-Felder fehlen; `13` = Session offline/error; `53` = finish-gate-Skript fehlt; `55` = readiness-summary-Skript fehlt; `56` = Stream BLOCKED, nur BLOCKER erlaubt | `<stream_id> <status_token> [message]` wobei status_token ∈ {ACK, BLOCKER, READY_FOR_REVIEW} | `AGENT_FINISH_GATE_BIN`, `PR_READINESS_SUMMARY_BIN` | stdout: Status-Meldungen; Dateien: `<git-common-dir>/agent-state/streams/<stream_id>.json` (aktualisiert); GitHub-Issue-Kommentar via `gh api` | `lib/opencode-wake-common.sh`, `lib/captain-hard-automation.sh` | Agent postet Status-Updates (ACK/BLOCKER/READY_FOR_REVIEW) als GitHub-Issue-Kommentare und aktualisiert den Stream-State. Bei READY_FOR_REVIEW werden finish-gate und pr-readiness-summary als Vorbedingung geprueft. |
| `scripts/captain/agent-finish-gate.sh` | Alle Unter-Gates bestanden; Finish-Evidence-JSON geschrieben | `2` = falsche Args; `3` = verify-only und Evidence fehlt/stale; `14` = Working-Tree nicht clean; `20` = AGENT_ID/TASK_KEY fehlen; `30` = git/jq fehlen; rc des fehlerhaften Unter-Gates sonst | `[--enforce\|--verify-only] [--skip-sonar]` | `AGENT_ID` (oder `.agent-id`), `TASK_KEY` (oder `.task-key`) | stdout: Fortschritts-Meldungen; Dateien: `<git-common-dir>/agent-state/local-gates/<agent_id>--<task_key>--finish.json` | `scripts/captain/write-agent-evidence.sh`, ruft auf: `legacy-ratchet-gate.sh`, `check-test-taxonomy.sh`, `check-cr-adr-reference-gate.sh`, `agent-class-test-gate.sh`, `sonar-agent-gate.sh` | Orchestriert alle verpflichtenden lokalen Gates (legacy-ratchet, test-taxonomy, CR/ADR-Referenz, class-test, Sonar) vor PR-Ready-Meldung. Schreibt bei Erfolg/Fehler ein Evidence-JSON. |
| `scripts/captain/sonar-agent-gate.sh` | Sonar-Quality-Gate bestanden (oder optional uebersprungen); Evidence geschrieben | `1` = Sonar gate failed / coverage fehlt/stale; `2` = falsche Args; `24` = Lock busy; `30` = git/jq fehlen; rc von sonar-gate.sh sonst | `[--require\|--optional] [--skip-tests] [--max-coverage-age-minutes <n>]` | `SONAR_TOKEN`, `CURAOPS_REQUIRE_SONAR`, `CURAOPS_SONAR_TOKEN_FILE`, `CURAOPS_MAX_COVERAGE_AGE_MINUTES` | stdout: Status-Meldungen; Dateien: `<git-common-dir>/agent-state/local-gates/<agent_id>--<task_key>--sonar.json`, `coverage.xml.sha` (wenn Tests liefen), Lock unter `<git-common-dir>/agent-state/locks/sonar.lock/` | `scripts/captain/write-agent-evidence.sh`, `scripts/sonar-gate.sh` | Serialisiert lokale SonarQube-Ausfuehrung (Lock-Datei), validiert coverage.xml-Alter und SHA-Sidecar, schreibt Evidence. Im optional-Modus wird bei fehlendem Token/coverage graceful geskippt. |
| `scripts/captain/pr-readiness-summary.sh` | GO: Alle geforderten lokalen Evidenzen sind aktuell und pass; NO-GO wird gemeldet aber rc=0 (ausser --verify) | `1` = --verify und Decision=NO-GO; `2` = falsche Args; `20` = AGENT_ID/TASK_KEY fehlen; `30` = git/jq fehlen | `[--verify] [--markdown]` | `AGENT_ID` (oder `.agent-id`), `TASK_KEY` (oder `.task-key`), `CURAOPS_REQUIRE_SONAR` | stdout: Einzeilige Key=Value-Zusammenfassung oder Markdown-Tabelle (mit --markdown) | keine (selbstaendig, liest Evidence-Dateien) | Liest finish-, test- und sonar-Evidence-JSONs und gibt GO/NO-GO-Entscheidung aus. Mit --verify schlaegt es bei NO-GO fehl; mit --markdown formatiert es als Tabelle. |
| `scripts/captain/write-agent-evidence.sh` | Evidence-JSON erfolgreich geschrieben | `2` = fehlende/ungueltige Pflicht-Args (--gate, --status, --skipped, --required); `30` = git/jq fehlen | `--gate <name> --status <pass\|fail> [--summary <text>] [--detail <text>] [--output <path>] [--command <text>] [--exit-code <code>] [--skipped <true\|false>] [--required <true\|false>]` | `AGENT_ID` (oder `.agent-id`, default: `captain`), `TASK_KEY` (oder `.task-key`, default: `unknown-task`) | Dateien: `<git-common-dir>/agent-state/local-gates/<agent_id>--<task_key>--<gate>.json` (oder --output-Pfad); stdout: `agent-evidence: <status> gate=<gate> evidence=<path>` | keine (selbstaendig) | Schreibt ein strukturiertes Evidence-JSON (status, gate, head_sha, changed_files, Metadaten) in das lokale agent-state-Verzeichnis. Universelles Hilfsskript fuer alle Gate-Evidenzen. |
| `scripts/sonar-gate.sh` | SonarQube Quality Gate bestanden (oder --skip-gate); Tests+Coverage generiert | `1` = SONAR_TOKEN fehlt / SonarQube nicht erreichbar / coverage.xml fehlt / Quality Gate fehlgeschlagen | `[--skip-tests] [--skip-gate]` | `SONAR_HOST_URL` (default: `http://127.0.0.1:9000`), `SONAR_TOKEN`, `PYTHON_BIN` | stdout: farbige Status-Meldungen; Dateien: `coverage.xml` (wenn Tests liefen); Docker-Sonar-Scan | `scripts/sonar-env.sh` | Lokaler SonarQube-Quality-Gate: fuehrt pytest+Coverage aus (oder nutzt vorhandene coverage.xml), startet Docker-basierten Sonar-Scanner, prueft Quality Gate. Haupt-Sonar-Einstiegspunkt. |
| `scripts/agent/agent-class-test-gate.sh` | Klassengesteuerte Test-Gate bestanden; Evidence geschrieben | `1` = Gate fehlgeschlagen / fehlende Klassifikation / kein Agent-Worktree / keine Changes; `2` = falsche Args; `3` = verify-only, Evidence fehlt/stale; `30` = Abhaengigkeiten fehlen (gh/git/jq/python3) | `[--enforce\|--verify-only] [--skip-cache]` | (ueber libs): `AGENT_ID`, `TASK_KEY`, diverse Agent-Context-Vars | stdout: Fortschritts-Meldungen; Dateien: `<git-common-dir>/agent-state/test-gates/<agent_id>--<task_key>.json` | `lib/agent-common.sh`, `lib/captain-hard-automation.sh` | Loest Aenderungsklasse (A/B2/B1/C) aus GitHub-Issue-Body, bestimmt noetige Tests (pytest, shellcheck, frontend-lint/test) basierend auf beruehrten Dateien und Klasse, fuehrt sie aus und schreibt Evidence. C/docs-only ueberspringt Tests. |
| `scripts/agent/agent-open-pr.sh` | PR erfolgreich erstellt, Branch gepusht, ggf. Codex-Kommentar und Issue-Notifikation | `1` = falsche Args/ungueltige Klassifikation; `5` = ungelesene Captain-Nachrichten; `20` = Agent-Kontext-Fehler; `24` = --no-preflight ohne Bypass; `25` = detached HEAD; `26` = keine Changes; `30` = gh/git fehlen; `35` = Push fehlgeschlagen; `40` = PR-Erstellung fehlgeschlagen; `50` = Klassifikation ungueltig; `52` = Artefakte fehlen; `53` = Gate-Skript fehlt; `54` = Gate fehlgeschlagen; `55` = Readiness-Skript fehlt | `[--no-preflight] [<title>]` | `AGENT_ID`, `TASK_KEY`, `AGENT_SYNC_BIN`, `AGENT_PREFLIGHT_BIN`, `AGENT_CLASS_TEST_GATE_BIN`, `AGENT_FINISH_GATE_BIN`, `PR_READINESS_SUMMARY_BIN`, `CURAOPS_CAPTAIN_HARD_AUTOMATION_BYPASS` | stdout: Fortschritts-Meldungen; GitHub: PR erstellt, ggf. Codex-Kommentar, Issue-Kommentar | `lib/agent-common.sh`, `lib/captain-hard-automation.sh`; ruft auf: `agent-sync.sh`, `agent-preflight.sh`, `agent-class-test-gate.sh`, `agent-finish-gate.sh`, `pr-readiness-summary.sh` | Orchestriert kompletten PR-Workflow: Inbox-Sync, Preflight, Klassifikation+Artefakt-Validierung, class-test-gate, finish-gate, readiness-summary, Branch-Push, PR-Erstellung mit Review-Header im Body. Haupt-PR-Erstellungs-Skript fuer Agenten. |
| `scripts/agent/captain-merge-preflight.sh` | PREFLIGHT_OK: PR ist valide (open, nicht draft, Token+Artefakte+Evidence pruefbar) | `1` = falsche Args / PR nicht open / Draft / falscher Base-Branch / Token fehlt / Governance-Anker fehlt / QA-Artefakt fehlt/stale / TESTS_REF fehlt/mismatch; `20` = gh/git/jq fehlen | `<pr_number> [output_file]` | (keine zusaetzlichen) | stdout: `PREFLIGHT_OK pr=... token=... head=...`; Dateien: optionales `output_file` mit Key=Value-Zeilen (pr, title, base, head_branch, head_sha, token, qa_artifact, merge_state, thread_id, pr_url, ...) | `lib/captain-telemetry.sh` | Captain-seitiger Merge-Preflight: prueft PR-Metadaten (State, Draft, Base-Branch), Governance-Token im Titel, ADR/CR/PC-Artefakt im Branch, lokale QA-Evidence (finish + class-test) gegen HEAD-SHA, und TESTS_REF-Konsistenz. Schreibt Ergebnis optional in Output-Datei. |
